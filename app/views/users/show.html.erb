<pre>
Name: <%= @user.name %>
Role: <%= @user.role_name %>

Contracts:
	Sold  : <%= @user.sold_contracts.count %>
	Bought: <%= @user.bought_contracts.count %>

Debt: $<%= @user.debt %>
<% if @user.salary_contract %>
Salary (expected): $<%= @user.salary_contract.next_amount_estimate %> at <%= @user.salary_contract.next_payment_date %>
<% end %>

<%= link_to 'Receive money transfer from client', "/money_transfers/new?receiver_id=#{@user.id}" %>
<%= link_to 'Send money transfer', "/money_transfers/new?sender_id=#{@user.id}" %>
<%= link_to 'Add contract' %>

</pre>
